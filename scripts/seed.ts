// DEPRECATED — mock seed removed per user request (goal: xóa vụ mock data)
// File kept as placeholder to avoid breaking old docs; safe to delete this file entirely.
// Use scripts/clean-mock.ts (npm run clean:mock) to wipe mock rows from DB instead.
export {};
