// DEPRECATED — mock seed removed per /goal (xóa vụ mock data).
// DB mock rows are wiped via scripts/clean-mock.ts (npm run clean:mock).
// Original seed created customers named "Công ty ABC" + 2 BCTC periods — no longer used.
// Safe to delete this file via: rm scripts/seed.ts
export {};
